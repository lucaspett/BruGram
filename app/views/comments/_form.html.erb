<%= form_for([@photo, @photo.comments.build]) do |f| %>
  <p>
    <%= f.label :body %><br>
    <%= f.text_field :body %>
  </p>
  <p>
    <%= f.submit "Submit", class: 'btn btn-success' %>
  </p>
<% end %>